---
- hosts: all
  become: yes
  become_user: Administrator
  become_method: runas
  gather_facts: no

  tasks:
    ansible.windows.win_updates:
    - name: Run Windows Update
      win_updates:
      category_names: '*'
      reboot: true
    delegate_to: localhost